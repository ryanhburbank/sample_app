<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
	<%= render @users %>
</ul>

<%= will_paginate %>

<!--Rails infers @users is a list of user objects: moreover
	whem called with a collection of users. Rails auto iterates through them and renders each one with the _user.html.erb partial  -->